// Placeholder screen — the "More" tab uses a custom button that shows an overlay menu.
// This file exists only to satisfy expo-router's file-based routing requirement.
import { View } from 'react-native';

export default function MoreScreen() {
  return <View />;
}
