-- Add dram_recipe_id to food_log to link a logged food entry back to a recipe.
-- Column was added manually on prod (2026-03-29).
-- This file is intentionally empty — the migration is handled in migrate.ts
-- to avoid stored-procedure delimiter issues with multipleStatements mode.
SELECT 1;
